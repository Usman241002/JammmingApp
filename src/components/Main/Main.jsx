import React from "react";
import '../../styles/Main.css';

export default function Hero() {
  return(
    <main className = "flexContainer">
      <section id = "results" className = "flexContainer">

      </section>

      <section id = "playlist" className = "flexContainer">

      </section>
    </main>
  );
}
